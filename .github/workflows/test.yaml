name: Test
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test:
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Test
        run: make test
